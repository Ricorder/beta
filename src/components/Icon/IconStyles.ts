import { StyleSheet } from 'react-native';
import { grayLight } from '../../constants/Constants';

export const s = StyleSheet.create({
	icon: {
		width: '13%',
		backgroundColor: grayLight,
		borderRadius: 13,
		alignItems: 'center',
		justifyContent: 'center',
		marginVertical: 20,
		paddingVertical: 10,
		paddingHorizontal: 20,
	},
})
